{% extends "index" %}

{% set entry = entry ?? craft.entries()
	.section('pages')
	.slug('home')
	.status(null)
	.canonicalsOnly()
	.one() %}

{% block content %}
	{% include '_includes/_partials/renderContentBlocks' with {
		entry: entry
	} only %}
{% endblock %}
