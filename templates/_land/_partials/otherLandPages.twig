{% set relatedLandPages = craft.entries()
	.section('land')
	.type('landServices')
	.site(currentSite)
	.status(null)
	.id(['not', entry.id])
	.limit(null)
	.all() %}

{% include '_includes/_partials/relatedPages' with {
	relatedEntries: relatedLandPages,
	heading: 'Explore more',
	intro: 'Browse the other land services pages to learn more about Crosby’s energy and development opportunities.'
} only %}
