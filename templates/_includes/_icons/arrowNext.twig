<button class="btn icon arrow arrow-next pointer-events-auto {% if nextArrowClass is defined %}{{ nextArrowClass }}{% endif %}"{% if nextArrowId is defined %} id="{{ nextArrowId }}"{% endif %}>
	{% include "_includes/_icons/arrowBase" %}
</button>
