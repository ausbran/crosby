<button class="btn icon arrow arrow-prev rotate-180 pointer-events-auto {% if prevArrowClass is defined %}{{ prevArrowClass }}{% endif %}"{% if prevArrowId is defined %} id="{{ prevArrowId }}"{% endif %}>
	{% include "_includes/_icons/arrowBase" %}
</button>
